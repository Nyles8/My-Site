---
import { Button, FormInput, FormSelect, FormTextarea } from '@components/odyssey-theme';

const selectOptions = [
  "Google Search",
  "Social Media",
  "Email",
]

---

<form name="Contact Form" method="POST" action="/contact-thank-you">
  <FormInput type="text" label="姓名" name="Name" placeholder="John Doe" />
  <FormInput type="email" label="邮箱" name="Email" placeholder="hello@example.com" />
  <FormSelect label="在哪了解到我的项目?" options={selectOptions} />
  <FormTextarea label="消息" name="Message" placeholder="Your message here..." />
  <Button>发送</Button>
</form>

<style>
  form {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }
</style>